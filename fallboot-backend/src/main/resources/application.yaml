spring:
  application:
    name: fallboot
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: health,threaddump,metrics